---
name: InputDatePicker
route: /input-date-picker
menu: Components
---

import { Playground, Props } from 'docz'
import { Value } from 'react-values'
import InputDatePicker from '.'

# InputDatePicker

<Playground>
  <Value>
    {({ value, set }) => (
      <div style={{ height: 350 }}>
        <InputDatePicker
          value={value}
          onChange={set}
        />
        <p>
          <strong>Date selected:</strong>
          {' '}
          {value ? value.toString() : 'null'}
        </p>
      </div>
    )}
  </Value>
</Playground>

## Usage

```js
import { InputDatePicker } from 'polvokit'

function Example () {
  return (
    <InputDatePicker
      value={value}
      onChange={onChange}
    />
  )
}
```

## Props

<Props of={InputDatePicker} />
