---
name: Calendar
route: /calendar
menu: Components
---

import { Playground, Props } from 'docz'
import { Value } from 'react-values'
import { isBefore, isSameDay } from 'date-fns'
import Calendar from '.'

# Calendar

<Playground>
  <Value>
    {({ value, set }) => (
      <Calendar
        value={value}
        onChange={set}
      />
    )}
  </Value>
</Playground>

### Disable days

You can disable some days by using the `isDaySelectable` prop:

<Playground>
  <p>
    Weekend days are disabled:
  </p>
  <Value>
    {({ value, set }) => (
      <Calendar
        value={value}
        onChange={set}
        isDaySelectable={date => ![0, 6].includes(date.getDay())}
      />
    )}
  </Value>
</Playground>

### Range

By using the prop `isDayWithinRange`, you can specify
a range area of days in your calendar.

This feature is use in the `InputDatePicker` component.

<Playground>
  <Calendar
    onChange={() => null}
    isDayWithinRange={(date, hoveredDate) => {
      return date.getDate() >= 10 && date.getDate() <= 20
    }}
  />
</Playground>

## Usage

```js
import { Calendar } from 'polvokit'

<Calendar
  value={new Date()} // selected date
  onChange={onChange} // change handler
/>
```

## i18n

You can customize the messages with `ConfigProvider`. See the polvokit's i18n section.

## Props

<Props of={Calendar} />
