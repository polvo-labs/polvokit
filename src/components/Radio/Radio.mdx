---
name: Radio
route: /radio
menu: Forms
---

import { Playground, Props } from 'docz'
import { VerticalSpacer, Option } from '../..'
import Radio from '.'

# Radio

Just an alias for `<Option type='radio' {...props} />`.

<Playground>
  <VerticalSpacer>
    <Radio
      name='radio'
      label='Check it'
    />
    <Radio
      name='radio'
      label='Checked input'
      defaultChecked
    />
  </VerticalSpacer>
</Playground>

## Usage

```js
import { Radio } from 'polvokit'

<Radio
  label='Radio label'
/>
```

## Props

It supports all props from `<input />`, plus:

<Props of={Option} />
